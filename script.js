document.addEventListener('DOMContentLoaded', () => {
    const upButton = document.getElementById('up-button');
    const downButton = document.getElementById('down-button');
    const menuButton = document.getElementById('menu-button');
    const pttButton = document.getElementById('ptt-button');
    const searchButton = document.getElementById('search-button');

    upButton.addEventListener('click', () => {
        console.log('UP Button Clicked');
        // Tambahkan logika untuk tombol UP
    });

    downButton.addEventListener('click', () => {
        console.log('DOWN Button Clicked');
        // Tambahkan logika untuk tombol DOWN
    });

    menuButton.addEventListener('click', () => {
        console.log('MENU Button Clicked');
        // Tambahkan logika untuk tombol MENU
    });

    pttButton.addEventListener('click', () => {
        console.log('PTT Button Clicked');
        // Tambahkan logika untuk tombol PTT
    });

    searchButton.addEventListener('click', () => {
        console.log('SEARCH Button Clicked');
        // Tambahkan logika untuk tombol SEARCH
    });
});
